<p id="notice"><%= notice %></p>

<p>
  <strong>Mark:</strong>
  <%= @mark.mark %>
</p>

<p>
  <strong>Subject:</strong>
  <%= Subject.find(@mark.subject_id).name %>
</p>

<p>
  <strong>Student:</strong>
  <%= Student.find(@mark.student_id).name %>
</p>
<%if current_user.has_role? :teacher%>
<%= link_to 'Edit', edit_mark_path(@mark) %> |<%end%>
<%= link_to 'Back', marks_path %>
